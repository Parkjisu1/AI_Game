## =============================================================
## 빌드 오더 (build_order.yaml)
## Project  : SLG-TowerDefense
## Platform : playable
## Generated: 2026-02-22
## =============================================================

platform: playable
note: >
  Playable은 단일 HTML 파일로 구성되므로 Phase 분할 없음.
  단일 태스크로 playable-coder에게 할당.

phases:
  - phase: 0
    description: "플레이어블 광고 HTML5 단일 파일 생성"
    nodes:
      - nodeId: "TowerDefensePlayable"
        assignee: "playable-coder"
        output: "E:/AI/projects/SLG-TowerDefense/output/playable.html"
        reference_yaml: "E:/AI/projects/SLG-TowerDefense/designs/nodes/TowerDefensePlayable.yaml"
        depends_on: []

validation:
  assignee: "validator"
  stages:
    - stage: 1
      name: "Isolation"
      check: "외부 HTTP 요청 없음 (CDN, 이미지 URL 등)"
    - stage: 2
      name: "Interaction"
      check: "touchstart + mousedown 이벤트 핸들러 존재"
    - stage: 3
      name: "CTA"
      check: "Install 버튼 + 클릭 핸들러 + window.open 존재"
    - stage: 4
      name: "Size"
      check: "파일 크기 < 2MB (Facebook 기준)"
